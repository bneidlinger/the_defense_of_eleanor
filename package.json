{
  "name": "siegefield-io",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Siegefield.io — endless medieval fortress survival builder (MVP 0 prototype)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
